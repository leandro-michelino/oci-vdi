# Create a fresh local repository
mkdir oci-secure-desktops
cd oci-secure-desktops

# Copy the files from this bundle into the new folder first, then run:
git init
git branch -M main

git add .
git commit -m "Initial OCI Secure Desktops Terraform baseline"

# Create your new remote repository first, then attach it:
git remote add origin https://github.com/<YOUR_GITHUB_USER>/oci-secure-desktops.git
git push -u origin main

# Terraform workflow
cp terraform.tfvars.example terraform.tfvars
terraform init
terraform fmt -recursive
terraform validate
terraform plan -out tfplan
